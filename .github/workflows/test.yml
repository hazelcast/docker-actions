name: Test All
on:
  pull_request:
  workflow_dispatch:

jobs:
  test-actions:
    uses: ./.github/workflows/test-actions.yml
    secrets: inherit

  test-scripts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test scripts
        run: ./test_scripts.sh
